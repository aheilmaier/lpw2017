#!/usr/bin/env perl

use strict;
use warnings;
use feature 'say';
use Data::Dump 'pp';


my $num = 500;

my $r_num = \$num;

# 1. output the integer from $r_num

# 2. create an anonymous reference to a scalar

